# Pinned WEB-UI dependencies (M9): Streamlit + its transitive additions on
# top of the base requirements.txt pins. Installed ONLY in the `web` Docker
# stage / by operators who want the pilot UI - the CLI image and native CLI
# installs never need these.
#
# Regenerate intentionally (see docs/RELEASE.md): in a clean Python 3.13 venv
# install -r requirements.txt then streamlit==<pin>, `pip freeze`, and keep
# the lines NOT already in requirements.txt.
GitPython==3.1.51
Jinja2==3.1.6
MarkupSafe==3.0.3
altair==6.2.2
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.4
gitdb==4.0.12
httptools==0.8.0
itsdangerous==2.2.0
jsonschema-specifications==2025.9.1
jsonschema==4.26.0
narwhals==2.24.0
packaging==26.2
pillow==12.3.0
protobuf==7.35.1
pyarrow==24.0.0
pydeck==0.9.3
python-multipart==0.0.32
referencing==0.37.0
rpds-py==2026.6.3
smmap==5.0.3
starlette==1.3.1
streamlit==1.59.2
toml==0.10.2
uvicorn==0.51.0
